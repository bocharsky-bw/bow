{% extends 'BWMainBundle:Layout/Backend:layout.html.twig' %}

{% block title %}Список статей {{ parent() }}{% endblock %}

{% block pre_content %}
    <div class="form-buttons">
        <a class="btn btn-primary icon-plus before-padding" href="{{ path('admin_article_add') }}" title="Добавить новую запись">Создать</a>
    </div>
{% endblock %}

{% block content %}
    <div class="bw_container">
        <div class="bw_row">
            <h1><i class="icon-file-text-alt"></i> Список статей</h1>
        </div>
    </div>
    {% if articles %}
        <div class="main-table">   
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th style="width: 5%" class="ta_c">№</th>
                        <th>Название</th>
                        <th style="width: 5%" class="ta_c">Статус</th>
                        <th style="width: 10%">Дата создания</th>
                        <th style="width: 10%">Дата изменения</th>
                        <th style="width: 5%" class="ta_c">Язык</th>
                        <th style="width: 5%" class="ta_c">ID</th>
                    </tr>
                </thead> 
                {% for article in articles %}
                    <tbody>
                        <tr>
                            <td class="ta_c">{{ loop.index }}</td>
                            <td>
                                <a href="{{ path('admin_article_edit', {'id': article.id}) }}" title="Редактировать статью">{{ article.heading }}</a> 
                            </td>
                            <td class="ta_c">
                                <a href="{{ path('admin_article_toogle_published', {'id': article.id}) }}">
                                    {% if article.published %}
                                        <i class="green icon-ok-sign" style="color: green;" title="Снять с публикации"></i>
                                    {% else %}
                                        <i class="red icon-minus-sign" style="color: red;" title="Опубликовать"></i>
                                    {% endif %}
                                </a>
                            </td>
                            <td>
                                {{ article.created|date('d/m/Y') }}
                            </td>
                            <td>
                                {{ article.updated|date('d/m/Y') }}
                            </td>
                            <td class="ta_c">
                                {% if article.lang %}
                                    <abbr title="{{ article.lang.name }}">{{ article.lang.sign }}</abbr>
                                {% else %}
                                    ~
                                {% endif %}
                            </td>
                            <td class="ta_c">
                                <span title="ID статьи в БД">{{ article.id }}</span>
                            </td>
                        </tr>
                    </tbody>
                {% endfor %}
            </table>
        </div>
    {% else %}
        <div class="ta_c">Записи отсутствуют</div>
    {% endif %}
{% endblock %}