{% extends 'BWMainBundle:Layout/Backend:layout.html.twig' %}

{% block title %}Список страниц {{ parent() }}{% endblock %}

{% block pre_content %}
    <div class="form-buttons">
        <a class="btn btn-primary icon-plus before-padding" href="{{ path('admin_page_add') }}" title="Добавить новую запись">Создать</a>
    </div>
{% endblock %}

{% block content %}
    <div class="bw_container">
        <div class="bw_row">
            <h1><i class="icon-file-text"></i> Список страниц</h1>
        </div>
    </div>
    {% if pages %}
        <div class="main-table">   
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th style="width: 5%" class="ta_c">№</th>
                        <th>Название</th>
                        <th style="width: 5%" class="ta_c">Статус</th>
                        <th style="width: 10%">Дата создания</th>
                        <th style="width: 10%">Дата изменения</th>
                        <th style="width: 5%" class="ta_c">Язык</th>
                        <th style="width: 5%" class="ta_c">ID</th>
                    </tr>
                </thead> 
                {% for page in pages %}
                    <tbody>
                        <tr>
                            <td class="ta_c">{{ loop.index }}</td>
                            <td>
                                <a href="{{ path('admin_page_edit', {'id': page.id}) }}" title="Редактировать статью">{{ page.heading }}</a> 
                            </td>
                            <td class="ta_c">
                                <a href="{{ path('admin_page_toogle_published', {'id': page.id}) }}">
                                    {% if page.published %}
                                        <i class="green icon-ok-sign" style="color: green;" title="Снять с публикации"></i>
                                    {% else %}
                                        <i class="red icon-minus-sign" style="color: red;" title="Опубликовать"></i>
                                    {% endif %}
                                </a>
                            </td>
                            <td>
                                {{ page.created|date('d/m/Y') }}
                            </td>
                            <td>
                                {{ page.updated|date('d/m/Y') }}
                            </td>
                            <td class="ta_c">
                                {% if page.lang %}
                                    <abbr title="{{ page.lang.name }}">{{ page.lang.sign }}</abbr>
                                {% else %}
                                    ~
                                {% endif %}
                            </td>
                            <td class="ta_c">
                                <span title="ID статьи в БД">{{ page.id }}</span>
                            </td>
                        </tr>
                    </tbody>
                {% endfor %}
            </table>
        </div>
    {% else %}
        <div class="ta_c">Записи отсутствуют</div>
    {% endif %}
{% endblock %}