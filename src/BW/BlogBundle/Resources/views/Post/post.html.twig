{% extends 'BWMainBundle:Layout/Frontend:layout.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block description %}{{ post.metaDescription }}{% endblock %}

{% block content %}
    <h1>{{ post.heading }}</h1>
    <small>{{ post.created.format('d/m/Y в H:i') }}</small>
    <div>{{ post.content|raw }}</div>
{% endblock %}