<h1>Поздравляем с успешной регистрацией!</h1>
<p style="padding-bottom: 10px; font-size: 1.2em;">
    Вы были зарегистрированы на сайте <a href="http://{{ app.request.server.get('HTTP_HOST') }}" target="_blank" title="Перейти на сайт {{ app.request.server.get('HTTP_HOST') }}">{{ app.request.server.get('HTTP_HOST') }}</a>.<br>
    Ваш логин: <b>{{ user.username }}</b>.<br>
    Дополнительную информацию про свой аккаунт Вы сможете изменить в своем профиле на сайте после активации аккаунта.
</p>
<p style="padding-bottom: 10px; font-size: 1.2em;">
    Если Вы не активируете свой аккаунт в течении 5 дней он будет автоматически удален.<br>
    Для активации своего аккаунта перейдите по ссылке ниже:<br>
    <a href="{{ url('user_email_confirm', {'hash': user.hash}) }}">{{ url('user_email_confirm', {'hash': user.hash}) }}</a>
</p>
<p style="padding-bottom: 10px; font-size: 1.2em; color: #666">
    Не нужно отвечать на это письмо так как оно было сгенерировано автоматически.<br>
    Проигнорируйте данное письмо если Вы не регистрировались на сайте <a href="http://{{ app.request.server.get('HTTP_HOST') }}" target="_blank" title="Перейти на сайт {{ app.request.server.get('HTTP_HOST') }}">{{ app.request.server.get('HTTP_HOST') }}</a> и считаете что письмо отправлено вам случайно.
</p>